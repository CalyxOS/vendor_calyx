PRODUCT_VERSION_MAJOR := 6
PRODUCT_VERSION_QPR := 9
PRODUCT_VERSION_MONTH := 0
PRODUCT_VERSION_RELEASE := 50

CALYXOS_VERSION := $(PRODUCT_VERSION_MAJOR).$(PRODUCT_VERSION_QPR).$(PRODUCT_VERSION_MONTH).$(PRODUCT_VERSION_RELEASE)

ifneq ($(OFFICIAL_BUILD),true)
CALYXOS_VERSION := $(CALYXOS_VERSION)-UNOFFICIAL
endif

PRODUCT_PRODUCT_PROPERTIES += \
    ro.calyxos.version=$(CALYXOS_VERSION)

# BUILD_NUMBER
# See $top/calyx/scripts/release/version.sh
# last 2 of year,    yy * 10000000
# + PRODUCT_VERSION_MAJOR * 100000
# + PRODUCT_VERSION_QPR * 10000
# + PRODUCT_VERSION_MONTH * 10
# + PRODUCT_VERSION_RELEASE
# Examples:
# 6.9.0.50 -> 250690050
# 6.9.1.20 -> 250690120
